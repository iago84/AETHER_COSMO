[tool.ruff]
line-length = 100
target-version = "py311"
extend-select = ["E", "F", "I"]
exclude = [
  "aetherlab/data/outputs/*",
]

[tool.ruff.isort]
known-first-party = ["aetherlab"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

